{
  "name": "parcel-tailwind-boilerplate",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "yarn build",
    "clean": "rm -rf .cache dist",
    "serve": "parcel *.pug",
    "build": "parcel build *.pug",
    "release": "yarn clean && yarn build && release-it",
    "snap": "git add -A && git commit -m 'Snapshot'",
    "caddy": "caddy file-server --browse --root ./dist/ --listen localhost:1234"
  },
  "author": "Felix Fischer <kontakt@felixfischer.com>",
  "license": "AGPL-3.0-or-later",
  "dependencies": {
    "jquery": "^3.5.1",
    "style": "0.0.3"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "postcss-import": "^12.0.1",
    "postcss-nested": "^4.2.3",
    "pug": "^3.0.0",
    "release-it": "^14.2.0",
    "tailwindcss": "^1.8.2"
  },
  "release-it": {
    "github": {
      "release": false
    },
    "npm": {
      "publish": false
    }
  }
}
